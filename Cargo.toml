[workspace]
resolver = "2"
members = ["crates/holos-tda", "crates/holos-tda-py"]

# Profiles apply from the workspace root only; member profiles are ignored.
[profile.release]
lto = "thin"
codegen-units = 1

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
